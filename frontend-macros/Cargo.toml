[package]
name = "frontend-macros"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
commands = { path = "../commands" }
quote = "1.0"
syn = { version = "2.0", features = ["full"] }
